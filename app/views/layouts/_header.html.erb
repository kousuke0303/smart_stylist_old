<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="sm-menu hidden">
      <div class="top-space"></div>
      <div data-toggle="push-menu" role="button" tabindex="0" class="hamburger-menu">
        <span></span>
        <span></span>
        <span></span>
      </div>
      <div class="menu-logo">menu</div>
    </div>
    <div class="logo">
    <%= link_to "Smart Stylist", root_path, id: "logo" %>
    </div>
    <div>
      <nav>
        <ul class="nav navbar-nav navbar-right pc-menu">
          <% if logged_in? %>
            <% if current_user.admin? %>
              <li><%= link_to "ユーザー一覧", users_path %></li>
            <% end %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                メニュー <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "アカウント", current_user %></li>
                <li class="divider"></li>
                <li><%= link_to "利用規約", terms_of_service_path %></li>
                <li class="divider"></li>
                <li><%= link_to "ログアウト", logout_path, method: :delete %></li>
              </ul>
            </li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>

<script>
  $('.hamburger-menu').on('click', function() {

    // $('.sm_main_content').toggle();
    // $('.sm_sidebar').toggle();
    // $('.sm_sidebar').toggleClass('col-sm-2');
    // $('.sm_sidebar').toggleClass('sm_mobile_sidebar');
    // $('.sm_sidebar_menu').toggleClass('sm_mobile_sidebar_menu')
    // $('body').toggleClass('sm_sidebar_open');
  });
</script>